 

query posts($blogId:Float!,$postId:Float){
    fetchPosts(blogId:$blogId,postId:$postId){
        blogId
        id
        title
        details
        comments{
            details
            username
        }
        Likes{
            userId
            hasLiked
        }
    }
}